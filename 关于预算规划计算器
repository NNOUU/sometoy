应用目标是做一个建议的预算规划计算，我输入一个总预算和一个总天数，然后程序自动帮我将每天的平均预算计算出来（平均预算为 剩余预算/剩余天数），
而其中的剩余预算需要我输入每天实际所使用的预算后计算得出，而剩余天数可以手动更改，也可以自动计算，
为了添加保存功能，以便在关闭后记住上一次输入的数据，使用浏览器的localStorage API。
每次点击“计算”按钮时，它将更新 localStorage 中的数据。这样，即使在关闭浏览器后重新打开，它也会记住上一次的输入。
此外，添加了一个新的输入框用于每日实际预算消耗，并在计算时减去它，然后重新计算每日平均预算。
